name: Deploy Infrastructure and Configure EC2s

on:
  workflow_dispatch:  # manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Ansible playbook
        run: |
          ansible-playbook Ansible_Rules/EC2_server.yaml -i Ansible_Rules/inventory.ini


    # - name: Run infrastructure build and configure script
    #   working-directory: ./scripts
    #   run: |
    #     chmod +x ./infra_build_configure.sh
    #     ./infra_build_configure.sh
